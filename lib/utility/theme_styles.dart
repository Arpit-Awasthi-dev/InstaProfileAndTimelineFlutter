import 'package:flutter/material.dart';

Color primaryColor = Colors.deepOrangeAccent;

// default scaffold background color
Color defaultBackgroundColor(context) => Theme.of(context).scaffoldBackgroundColor;